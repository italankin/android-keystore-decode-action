name: 'Android keystore decode'
inputs:
  encoded_keystore:
    required: true

  keystore_name:
    required: true

  properties_name:
    required: true

  keystore_password:
    required: true

  key_alias:
    required: true

  key_alias_password:
    required: true

  output_dir:
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
